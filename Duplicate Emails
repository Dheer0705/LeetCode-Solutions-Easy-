import pandas as pd

def duplicate_emails(person: pd.DataFrame) -> pd.DataFrame:
    database = person[person.duplicated(subset=['email'])]['email'].unique()
    return pd.DataFrame({'Email': database})
