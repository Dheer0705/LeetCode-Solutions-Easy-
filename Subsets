class Solution:
    def subsets(self, nums: List[int]) -> List[List[int]]:
        arr = []
        for i in range(len(nums)+1):
            for combo in combinations(nums,i):
                arr.append(list(combo))
        return arr
